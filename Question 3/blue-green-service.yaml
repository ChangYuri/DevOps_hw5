# https://kubernetes.io/docs/concepts/services-networking/service/

apiVersion: v1
kind: Service
metadata:
  name: hw-two-service
spec:
  selector:
    app: nginx
    version: blue

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  
  type: NodePort
